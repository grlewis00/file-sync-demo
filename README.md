# file-sync-demo

This repository contains a single file with a sample implementation of the file upload portion of a file synchronisation system where a file is continually synchronised to a server. 